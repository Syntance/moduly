/** STUB typów eventów — podmień na @moduly/analytics-events. */
export type EcommerceItem = {
  item_id: string;
  item_name: string;
  price?: number;
  quantity?: number;
  [key: string]: unknown;
};
